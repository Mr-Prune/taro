<template>
  <div
    className="{{className}}"
    onClick="handleClick"
  >
    <slot></slot>
  </div>
</template>

<script>
  import {mergeProps} from '../../libs/utils'

  export default {

    props: ['className'],

    propsDefault: {
      className: 'taro_text'

    },

    data: () => ({

    }),

    onInit () {

      mergeProps(this)
    },

    handleClick (e) {

      this.$emit('click', e)
    }
  }
</script>

<style>
  .taro_text {

  }

</style>
